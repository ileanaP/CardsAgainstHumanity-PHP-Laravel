DATABASE NAME: 
laravel

COMMANDS TO INSTALL:

$ composer global require laravel/installer

$ composer install

$ php artisan key:generate

$ php artisan migrate

$ php artisan db:seed

$ php artisan passport:install
 
$ php artisan serve

DONE!

